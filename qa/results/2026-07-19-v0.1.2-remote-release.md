# v0.1.2 remote release acceptance — 2026-07-19

Status: **PASS for the public GitHub release path.** Visible Codex host placement, an outside-user
second-machine first run, and the separately reviewed core macOS navigation PR remain explicit
follow-ups.

Every host lifecycle used a new isolated home. All state and prompts were synthetic. No regular
Claude Code or Codex configuration, personal conversation, credential, or private Viventium state
was used.

## Published release

- Repository: `ProjectViventium/viventium-feelings` (public)
- Release commit: `aafb5adf047a9999fe13993070cd7a1e783524cd`
- Annotated tag: `v0.1.2`
- Release: `https://github.com/ProjectViventium/viventium-feelings/releases/tag/v0.1.2`
- Main-commit CI run `29699782078`: **PASS** on Node 20.11 and 22
- Tag CI run `29699910458`: **PASS** on Node 20.11 and 22

## Fresh remote-clone evidence

A new public GitHub clone of `main` resolved to the release commit and passed:

- `npm ci` and dependency audit with zero vulnerabilities;
- 96/96 tests on the current runtime and exact minimum Node 20.11.1;
- isolated native Claude/Codex install, list, update, optional-presence teardown, and removal;
- the complete real-Chromium dashboard journey with a clean console and local-only network.

A second new clone of the immutable `v0.1.2` tag independently resolved to the same commit and
passed 96/96 on current and exact-minimum Node, dependency audit, and both isolated native package
lifecycles.

## Remote marketplace lifecycle

| Host | Remote add | Install/list | Refresh/update | Remove/empty list |
| --- | --- | --- | --- | --- |
| Codex | **PASS**, Git source pinned to `v0.1.2` | **PASS**, version `0.1.2`, enabled, `ON_USE` authorization | **PASS** | **PASS** |
| Claude Code | **PASS**, public repository cloned over HTTPS | **PASS**, version `0.1.2`, enabled | **PASS**, already current | **PASS** |

## Immutable archive

- Attached archive: `viventium-feelings-0.1.2.tar.gz`
- SHA-256: `d444f12adc6d9f4121c56928bcc2b01256b6a92e68ed5bdae6b9c68e220fe916`
- The downloaded attachment passed its attached checksum.
- Embedded package, Claude, and Codex manifests all report `0.1.2`.

## Independent acceptance

Visible Claude Desktop Fable 5 Extra and ClaudeViv Opus 4.8 at xHigh effort both returned
**APPROVE** after independently re-opening the remediation. Neither found a remaining P0, P1, or
P2. Their evidence does not replace the browser, native-host, CI, or archive gates above.

The core macOS `Open Feelings` change is intentionally separate in
`ProjectViventium/viventium` pull request `#67`. Its automated checks are green, but required human
review and safe sequencing with concurrent helper work remain before merge. This plugin release
does not claim that unmerged core change as shipped.

## Remaining honest boundary

The public GitHub install and update path is release-ready and proven. Vendor-directory discovery,
visible Codex placement on every desktop surface, and an outside user's second-machine first-run
remain distribution and host-UI confidence work, not evidence relabeled as complete here.
