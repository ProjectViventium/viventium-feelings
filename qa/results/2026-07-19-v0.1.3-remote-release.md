# v0.1.3 remote release acceptance — 2026-07-19

Status: **PASS for the official public GitHub release path.** The published tag, archive, Claude
Code lifecycle, Codex lifecycle, automated suite, and real-browser journey were all exercised from
fresh public inputs after publication.

Every host lifecycle used a new isolated home. All state and prompts were synthetic. No regular
Claude Code or Codex configuration, personal conversation, credential, or private Viventium state
was changed.

## Published release

- Repository: `ProjectViventium/viventium-feelings` (public)
- Release commit: `f1364931612007c6ed37ab087f2760408b8caa1d`
- Annotated tag: `v0.1.3`
- Release: `https://github.com/ProjectViventium/viventium-feelings/releases/tag/v0.1.3`
- Main CI run `29705042475`: **PASS** on Node 20.11 and 22
- Tag CI run `29705089783`: **PASS** on Node 20.11 and 22

## Fresh public-tag evidence

A new clone of the public `v0.1.3` tag resolved to the release commit and passed:

- `npm ci` and dependency audit with zero vulnerabilities;
- 107/107 tests on the current runtime and exact minimum Node 20.11.1;
- static package validation;
- the complete real-Chromium dashboard journey with a clean console and local-only network.

## Remote marketplace lifecycle

| Host | Remote add | Install/list | Refresh/update | Remove/empty list |
| --- | --- | --- | --- | --- |
| Codex | **PASS**, public Git marketplace | **PASS**, version `0.1.3`, enabled, `ON_USE` authorization | **PASS** | **PASS** |
| Claude Code | **PASS**, public repository cloned over HTTPS | **PASS**, version `0.1.3`, enabled | **PASS**, already current | **PASS** |

The Claude manifest resolved its host-specific MCP server and the Codex manifest resolved the same
shared plugin root. The isolated profiles proved marketplace discovery and installation from the
public GitHub repository rather than a local checkout.

## Immutable archive

- Attached archive: `viventium-feelings-v0.1.3.tar.gz`
- SHA-256: `846f00093dee656f593c5690adbf50a2c00b6b72edb0df90a76423b50ff3cd8d`
- The downloaded GitHub attachment passed its attached checksum.
- Embedded package, Claude, and Codex manifests all report `0.1.3`.
- A public-safety scan found no local home paths, personal email addresses, private keys, or common
  token formats in the tagged archive.

## Independent acceptance

Visible Claude Desktop Fable 5 Extra returned **APPROVE** with no evidence-backed release blocker.
Two additional independent reviews returned **APPROVE** after reproducing the package suite,
validation, minimum-version, host-environment, artifact, and public-boundary checks.

Core Viventium status-menu parity is separately shipped on `ProjectViventium/viventium` main at
`d9bdb7182500f1e3a658cca7cb49b3a01477ef87`. Its pinned LibreChat component is
`7c702629599f5b229f9b49f6ea2f458c6981581a`.

## Honest capability boundary

The release supports Claude Code and Codex plugin hosts. It does not alter ordinary consumer Claude
or ChatGPT web conversations. The full Viventium macOS helper owns the operating-system status-menu
surface; Claude Code can additionally show the owned `V Feelings` status line after explicit opt-in,
while Codex exposes native plugin identity instead of claiming a third-party TUI status segment.

The public path is proven on fresh isolated profiles on the release machine. A genuinely independent
outside-user second-machine first run and vendor-directory discovery remain launch-confidence work,
not evidence relabeled as complete.
